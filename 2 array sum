#include <stdio.h>

int main() {
    int a[3] = {1, 2, 3};
    int b[3] = {4, 5, 6};
    int sum[3];

    for(int i = 0; i < 3; i++) {
        sum[i] = a[i] + b[i];
        printf("%d ", sum[i]);
    }
    return 0;
}
